# Anthropic API key for the AI tutor (optional).
# Without this, the /api/tutor route returns 503 and the UI falls back
# to the local rule-based basic-strategy advisor.
# Get a key at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=
